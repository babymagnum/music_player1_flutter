import 'package:flutter/material.dart';

class GridMenuView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Grid Menu View'),
      ),
    );
  }
}
